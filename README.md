# Project 2
markdown_content = """# Vector Operations Utility

Ця програма на мові C++ призначена для роботи з динамічними масивами (векторами). Вона надає користувачеві консольний інтерфейс для введення даних та виконання базових статистичних операцій, таких як пошук мінімального та максимального значень.

## Функціональність

Програма підтримує наступні можливості:
* **Введення вектора**: Користувач може вказати кількість елементів та ввести кожне значення вручну.
* **Пошук мінімуму**: Знаходження найменшого елемента у поточному векторі.
* **Пошук максимуму**: Знаходження найбільшого елемента у поточному векторі.
* **Обробка помилок**: Програма перевіряє, чи не є вектор порожнім перед спробою знайти екстремуми, та генерує відповідні винятки (`runtime_error`).
* **Консольне меню**: Зручна навігація за допомогою цифрових команд.

## Структура проекту

* `Header.h` — Заголовний файл, що містить логіку функцій введення та обробки даних.
* `Source.cpp` — Точка входу в програму з реалізацією циклу меню.
* `UnitTest1.cpp` — Модульні тести для перевірки коректності роботи алгоритмів (використовується Microsoft Visual Studio C++ Unit Testing Framework).

## Вимоги

* Компілятор з підтримкою стандарту C++11 або вище.
* Середовище розробки (наприклад, Visual Studio 2022).

## Як запустити

1.  Скомпілюйте файли `Header.h` та `Source.cpp`.
2.  Запустіть отриманий виконуваний файл.
3.  Дотримуйтесь інструкцій у консолі:
    * Спочатку оберіть пункт `1`, щоб заповнити вектор даними.
    * Після цього ви зможете знайти мінімальне (`2`) або максимальне (`3`) значення.

## Тестування

Проект включає модульні тести, які покривають:
1.  Перевірку правильності знаходження мінімуму/максимуму.
2.  Перевірку поведінки програми при роботі з порожніми даними.
"""

with open('README.md', 'w', encoding='utf-8') as f:
    f.write(markdown_content)
